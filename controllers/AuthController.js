const { user } = require('../models')

const Login = async (req, res) => {
    try {

    } catch(error) {
        throw error
    }
}

const Register = async () => {
    try {

    } catch (error) {
        throw error
    }
}